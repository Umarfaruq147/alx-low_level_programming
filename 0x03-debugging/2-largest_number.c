#include "main.h"

/**
 * largest_nmber - returns the largest of 3 nmbers
 * @a: first integer
 * @b: second integer
 * @c: third integer
 * Return: largest number
 */

int largest_number(int a, int b, int c)
{
	int largest;

	if (a > b)
	{
		if (ca > c)
		{
			largest = a;
		}
		else
		{
			largest = c;
		}
	}
	else if (b > c)
	{
		largest = b;
	}
	else
	{
		largest = b;
	}
	else
	{
		largest = c;
	}

	return (largest);
}
