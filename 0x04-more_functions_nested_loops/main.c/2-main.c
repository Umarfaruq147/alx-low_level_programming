#include "../main.h"

/**
 * main - check the code for ALX School students.
 * Return: Always 0 (Success)
 */

int main(void)
{
	print("%d\n", mul(98, 1024));
	printf("%d\n", mul(-402, 4096));
	return (0);
}
